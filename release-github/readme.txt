this script is written to populate the Microsoft security release the team has to look through every month. 

to use, 
1) place original Microsoft security release file into same directory as main.py
2) open script and check if the file name corresponds
3) run script

run script
python microsoft-security-releases/main.py